FROM golang:1.26-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY apps ./apps
RUN CGO_ENABLED=0 go build -trimpath -ldflags='-s -w' -o /out/mocklet-api ./apps/api/cmd/mocklet

FROM gcr.io/distroless/static-debian12:nonroot
COPY --from=build /out/mocklet-api /mocklet-api
USER nonroot:nonroot
EXPOSE 8080
ENTRYPOINT ["/mocklet-api"]

