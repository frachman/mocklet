# Next actions

1. Run an isolated decrypt/restore rehearsal from the homelab using the private restore key and record the result.
2. Keep the daily Mocklet backup and freshness-check timers monitored alongside Hookbin.
3. Revisit the process-local rate limiter before horizontal scaling or multi-instance deployment.
4. Run migration 003 and authenticated HTTP integration tests for scenario CRUD and runtime selection.
5. Complete the bounded M4 OpenAPI preview/import flow only after M3 verification.
6. After M3 and M4 ship, start the explicit 30-day acquisition gate: at least 20 organic mock creations and one qualitative user feedback item.
