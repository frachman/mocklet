services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mocklet
      POSTGRES_USER: mocklet
      POSTGRES_PASSWORD: mocklet_dev
    ports:
      - "5432:5432"
    volumes:
      - mocklet-postgres:/var/lib/postgresql/data
volumes:
  mocklet-postgres:

